@using WebBanSach.Models;
@model List<Sach>

<head>
    <style>
        #Avatar {
            border-style: dotted;
            margin-top: 30px;
            margin-right:30px;
        }

        div#body {
            margin-left: 15%;
            margin-right: 15%;
            margin-top: -0px;
            background-color: #e6c4c4ad;
        }

        #price {
            font-size: 16px;
            font-family: Roboto, Helvetica, Arial, sans-serif;
            color: #ff0000;
            font-weight: 500;
            text-align: center;
        }

        ul li {
            padding-left: 1px;
            list-style: none;
        }

        .menuPage {
            margin-left: 10%;
        }

        {
            margin-top: 10px;
            font-size: 20;
            font-family: Helvetica, Arial, Tahoma, sans-serif;
            text-align: center;
        }

        

        ul.pagination {
            display: inline-flex;
        }

        .menuPage li {
            margin-left: 2px;
        }

        .pagination-container {
            margin-left: 40%;
        }
    </style>
</head>
<h4 style="margin-left:25%; color:red; margin-top:20px;">Tác phẩm: </h4>
@foreach (var item in Model)
{
    <div id="Book">
        <div id="Avatar">
            <a href="@Url.Action("DetailsBook","User",new { @masach=item.MaS   })">
                <img src="@Url.Content("~/Images/" +item.TenFileAnh)" width="180" height="230" />
            </a>
        </div>



    </div>


}